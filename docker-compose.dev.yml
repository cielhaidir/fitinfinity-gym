version: "3.9"

services:
 
  app:
    image: fitinfinity
    container_name: fitinfinity-app-dev
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      mosquitto:
        condition: service_healthy
    ports:
      - "3100:3000"  # Pakai port 3100 untuk dev
    env_file:
      - .env.dev
    networks:
      - fitinfinity-dev-network

networks:
  fitinfinity-dev-network:
    driver: bridge
